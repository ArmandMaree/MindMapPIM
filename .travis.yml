language: java
jdk:
  - oraclejdk8

sudo: required

notifications:
  email: false

services:
  - rabbitmq
  - mongodb

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

script:
  - cd BusinessLogic/
  - ./gradlew test
  - cd ../Database/
  - ./gradlew test
  - cd ../Frontend/
  - ./gradlew test
  - cd ../GmailPolling/
  - ./gradlew test
  - cd ../Processing/
  - ./gradlew test